{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Create build folder",
      "type": "process",
      "command": "uv",
      "args": [
        "run",
        "python",
        "-c",
        "from pathlib import Path; p = Path('./build'); p.mkdir(exist_ok=True); (p / '.gitignore').write_text('# Generated by VS Code tasks.json\\n*\\n')"
      ]
    }
  ]
}
